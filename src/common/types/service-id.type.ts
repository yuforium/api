/**
 * A ServiceId is a unique identifier for any service that is internally managed by this Yuforium instance.
 * If a ServiceId is not present, it can be assumed that the service is external to this instance.
 */
export type ServiceId = string;